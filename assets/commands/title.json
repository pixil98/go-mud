{
    "version": 1,
    "id": "title",
    "spec": {
        "handler": "title",
        "config": {
            "new_title": "{{ .Inputs.text }}"
        },
        "inputs": [
            {"name": "text", "type": "string", "required": false, "rest": true}
        ]
    }
}
