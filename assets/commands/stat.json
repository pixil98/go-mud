{
    "version": 1,
    "id": "stat",
    "spec": {
        "handler": "score",
        "category": "information",
        "description": "View the stats of a player or mobile.",
        "targets": [
            {"name": "target", "type": ["player", "mobile"], "scope": ["room"], "input": "target", "not_found": "You don't see '{{ .Inputs.target }}' here."}
        ],
        "inputs": [
            {"name": "target", "type": "string", "required": true, "missing": "Stat whom?"}
        ]
    }
}
