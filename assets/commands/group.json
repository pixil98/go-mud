{
    "version": 1,
    "id": "group",
    "spec": {
        "handler": "group",
        "category": "group",
        "priority": 5,
        "description": "Show your group, or add/remove a follower from it.",
        "targets": [
            {
                "name": "target",
                "type": ["player"],
                "scope": ["room"],
                "input": "target",
                "optional": true,
                "not_found": "You don't see '{{ .Inputs.target }}' here."
            }
        ],
        "inputs": [
            {"name": "target", "type": "string", "required": false}
        ]
    }
}
