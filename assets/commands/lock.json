{
    "version": 1,
    "id": "lock",
    "spec": {
        "handler": "closure",
        "category": "interaction",
        "description": "Lock a door or container.",
        "config": {
            "action": "lock"
        },
        "targets": [
            {
                "name": "target",
                "types": ["object", "exit"],
                "scopes": ["inventory", "room"],
                "input": "target",
                "not_found": "You don't see '{{ .Inputs.target }}' to lock."
            }
        ],
        "inputs": [
            {"name": "target", "type": "string", "required": true, "missing": "Lock what?"}
        ]
    }
}
