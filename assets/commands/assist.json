{
    "version": 1,
    "id": "assist",
    "spec": {
        "handler": "assist",
        "category": "combat",
        "description": "Join another player's fight.",
        "targets": [
            {
                "name": "target",
                "type": ["player"],
                "scope": ["room"],
                "input": "target",
                "optional": true,
                "not_found": "You don't see '{{ .Inputs.target }}' here."
            }
        ],
        "inputs": [
            {"name": "target", "type": "string", "required": false}
        ]
    }
}
