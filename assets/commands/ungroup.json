{
    "version": 1,
    "id": "ungroup",
    "spec": {
        "handler": "ungroup",
        "category": "group",
        "description": "Leave or disband your group, or remove a specific member.",
        "targets": [
            {
                "name": "target",
                "type": ["player"],
                "scope": ["group"],
                "input": "target",
                "optional": true,
                "not_found": "'{{ .Inputs.target }}' is not in your group."
            }
        ],
        "inputs": [
            {"name": "target", "type": "string", "required": false}
        ]
    }
}
