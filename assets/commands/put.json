{
    "version": 1,
    "id": "put",
    "spec": {
        "handler": "put",
        "category": "items",
        "description": "Put an item into a container.",
        "targets": [
            {"name": "target", "type": "object", "scope": ["inventory"], "input": "item"},
            {"name": "container", "type": "object", "scope": ["room","inventory","equipment"], "input": "container"}
        ],
        "inputs": [
            {"name": "item", "type": "string", "required": true},
            {"name": "container", "type": "string", "required": true}
        ]
    }
}