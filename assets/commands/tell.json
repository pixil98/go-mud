{
    "version": 1,
    "id": "tell",
    "spec": {
        "handler": "message",
        "config": {
            "channel": "player-{{ .Args.target }}"
        },
        "params": [
            {"name": "target", "type": "player", "required": true},
            {"name": "text", "type": "string", "required": true, "rest": true}
        ]
    }
}
