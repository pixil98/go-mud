{
    "version": 1,
    "id": "open",
    "spec": {
        "handler": "closure",
        "category": "interaction",
        "description": "Open a door or container.",
        "config": {
            "action": "open",
            "target": "{{ .Inputs.target }}"
        },
        "inputs": [
            {"name": "target", "type": "string", "required": true, "missing": "Open what?"}
        ]
    }
}
