{
    "version": 1,
    "id": "look",
    "spec": {
        "handler": "look",
        "config": {},
        "targets": [
            {"name": "target", "type": "target", "scope": ["room", "inventory"], "input": "target", "optional": true}
        ],
        "inputs": [
            {"name": "target", "type": "string", "required": false}
        ]
    }
}
