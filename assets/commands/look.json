{
    "version": 1,
    "id": "look",
    "spec": {
        "handler": "look",
        "config": {
            "target": {
                "$resolve": "target",
                "$scope": "room",
                "$input": "target",
                "$optional": true
            }
        },
        "inputs": [
            {"name": "target", "type": "string", "required": false}
        ]
    }
}
