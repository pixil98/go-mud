{
    "version": 1,
    "id": "look",
    "spec": {
        "handler": "look",
        "category": "information",
        "description": "Look at your surroundings or examine something.",
        "priority": 10,
        "targets": [
            {"name": "target", "types": ["player", "mobile", "object"], "scopes": ["room", "inventory"], "input": "target", "optional": true, "not_found": "You don't see '{{ .Inputs.target }}' here."}
        ],
        "inputs": [
            {"name": "target", "type": "string", "required": false}
        ]
    }
}
