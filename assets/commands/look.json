{
    "version": 1,
    "id": "look",
    "spec": {
        "handler": "look",
        "category": "information",
        "description": "Look at your surroundings or examine something.",
        "targets": [
            {"name": "target", "type": ["player", "mobile", "object"], "scope": ["room", "inventory"], "input": "target", "optional": true}
        ],
        "inputs": [
            {"name": "target", "type": "string", "required": false}
        ]
    }
}
