- We should expose the current target they're fighting as a templatable thing for commands to use for defaulting in targets.
- Clean up unused PerformAttack/rollOneAttack/AttackResult from combat/manager.go (now only used by tests)
- Update docs (combat.md, world-design.md) for auto_use replacing autoattack
- A number of command handlers have their own interfaces that embed shared.Actor. They shouldn't unless they actually need a big interface.
- Remove StatSections from CharacterInstance and format everything from Actor and perks
- auto_use doesn't respect the number, it always does it every round
- auto_use attack doesn't show the proper hit message.